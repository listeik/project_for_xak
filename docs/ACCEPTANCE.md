# Проверка готовности приложения

## Расчёт

1. Открыть приложение: загрузить параметры организаторов и исходный редактируемый план.
2. Убедиться, что по каждому году выполняется равенство:
   `start_inventory + inflow - losses - served_total = end_inventory`.
3. Проверить отдельные строки подготовительной поставки: валовой объём, потери,
   источник, дата заказа, стоимость. Начальный запас не прибавляется к притоку повторно.
4. Изменить закупки одного года и получить новый расчёт с API.
5. Уменьшить поставки до возникновения дефицита: физический запас остаётся неотрицательным,
   дефицит показан отдельно, критический спрос имеет приоритет.
6. Увеличить закупки сверх мощности: увидеть источник, год и величину нарушения.
7. Переключить BASE на MANDATORY_STRESS при тех же решениях и сравнить результаты.
8. Отключить ZBO в обязательном стрессе: увидеть нарушение ограничения потерь с 2038 года.

## Оптимизация

Оптимизация закупок выполняется при фиксированных инвестиционных решениях, ставке
дисконтирования и начальном запасе. После завершения LP результат повторно проверяется
основным симулятором. Неисполнимый набор инвестиций или недостаточный начальный резерв
должны приводить к объяснению невозможности, а не к выдаче произвольного плана.

## Пользовательский путь

- Сохранить план в JSON, изменить форму и загрузить файл обратно.
- Экспортировать CSV/XLSX и сопоставить числа с годовой таблицей.
- Проверить сообщения об ошибках для отрицательного объёма и неизвестного сценария.
- Убедиться, что во время расчёта отображается ожидание, а ошибка API видна пользователю.
- Проверить интерфейс при ширине 1440 px и на узком экране.

## Автоматические проверки

Организаторские V01–V10 сохраняют исходные номера и смысл. Исполняемый протокол
добавляет E01–E32 (включая даты дозаказов, TOP, совместную мощность, частичное восстановление, Emergency и CAPEX): потери двух складов, границы CAPEX, лунные поставки, подготовительный
запас, баланс, LOW/HIGH, неверный ввод, нулевую выдачу, стрессовый потолок потерь,
фиксацию договоров, расширенный горизонт и предупреждения о дефиците.
Они не заменяют V01–V10. Запуск: `python tools/build_contest_evidence.py`.
Фактические результаты находятся в [validation_protocol.md](validation_protocol.md)
и `results/validation_results.json`.

У обычного набора трёх инвестиций общая стоимость 1790 млн у.е., поэтому превышение
1800 млн проверяется на синтетическом денежном потоке, поданном непосредственно
в функцию проверки CAPEX. Исходные стоимости организаторов при этом не меняются.

Кроме арифметики необходимы интеграционные проверки API, экспорта, оптимизации,
некорректного ввода и воспроизводимости. Сборка фронтенда должна пройти проверку типов.

## Выполненная проверка версии 2.0, 19 сентября 2026

- V01–V10 и E01–E15: 25/25 PASSED. Целостность исходного репозитория:
  10/10 проверок `tools/validate_reference_repo.py`.
- API: OpenAPI доступен; JSON/CSV полного отчёта согласованы по хешу плана и NPV;
  JSON содержит 25 фактических проверок и пригоден для повторного расчёта плана.
- CSV/XLSX обычного расчёта поддерживают 8 лет до 2042, включая добавленный канал.
- Неверные отрицательные объёмы, критический спрос выше общего, неполный горизонт
  и изменение зафиксированных инвестиций отклоняются валидацией.
- Сохранённый защищённый STRESS-план повторно загружен и оптимизирован:
  все 20 ранних обязательств сохранены, дефицита нет, ограничения выполнены.
  Изменение A в 2038 выявляется как `SUNK_CONTRACT_CHANGED_A_2038`.
- Через интерфейс получены три стратегии в BASE/STRESS, цена устойчивости 343,93 млн
  и предотвращённый дефицит 144,00 т; проверены стрессовый график, реестр рисков,
  чувствительность и применение/снятие собственного геополитического шока.
- В интерфейсе открыты канал F и 2042 год. Начальные демонстрационные заказы
  вызывают корректные предупреждения о переполнении. После оптимизации NPV
  11645,06 млн, сервис 100%, формальные ограничения выполнены, дефицит отсутствует.
  Возврат к 5 каналам и 6 годам после выбора 2042 не вызывает ошибки отображения.
- Импортирован полный `results/contest_report.json` размером больше 3 МБ,
  затем `results/reserve_base_plan.json`; подпись графика отражает резерв 60 дней.
- Интерфейс визуально проверен на широком и узком экранах; ошибок JavaScript
  в проверенных переходах не обнаружено. Проверка TypeScript и production-сборка пройдены.
- Браузерный запрос конкурсного JSON получил HTTP 200 без ошибки приложения;
  интерфейс подтвердил передачу отчёта браузеру для скачивания.
  Событие фактического сохранения файла инструмент встроенного браузера не подтвердил;
  проверка содержимого файла выполнена непосредственно через API. Это ограничение
  браузерной проверки, а не подтверждение сохранения в пользовательскую папку.

Эти результаты относятся к локальной версии 2.0. Они не подтверждают её публикацию
на Render и не заменяют проверку после будущего деплоя. Сборка содержит предупреждение
о размере чанка ECharts (около 631 КБ до gzip); это не ошибка сборки.


## Фактическая проверка 2.1-recourse (2026-09-19)

- V01–V10 и E01–E26: 36/36. Проверены даты B/E, сохранение истории и обязательств, совместная мощность, недоступный C, TOP без двойного списания, Emergency не более двух лет подряд и честное частичное восстановление.
- Исходный BASE: NPV 8912,49; неизменный STRESS: дефицит 147,83 т. После реакции: NPV 10507,32, дефицита и формальных нарушений нет. B в 2038 получает отдельный дозаказ 55,66 т с 3 мая.
- При удвоенном спросе с 2038 и нулевой доставке B: используются E в 2039/2040; остаточные критический дефицит и нарушения резерва явно сохраняются.
- Явная бронь B 100 т/год в 2038 используется дозаказом без повторного платежа за бронь. Повторная адаптация сохраняет исходные обязательства и NPV.
- `/adapt`, `/calculate`, `/export` (JSON/CSV/XLSX), HTTP 422 для отрицательных дозаказов и отсутствующей ZBO, запрет предварительного LP для плана реакции проверены через TestClient.
- Сохранённые исходный адаптированный и подготовленные BASE/STRESS планы повторно загружены: NPV и отсутствие дефицита подтверждены.
- В браузере проверены четыре шага, применение реакции, отдельный дозаказ B, возврат к исходному BASE, четыре строки сравнения и открытие подготовленного STRESS. На ширине 1440 и 390 px страницы не выходят за экран; широкие таблицы прокручиваются внутри блока.
- TypeScript и production-сборка прошли. Известное предупреждение о размере ECharts остаётся; ошибок JavaScript в проверенных переходах нет.
- Конкурсный JSON подготовленного STRESS запрошен через браузер: сервер вернул HTTP 200. Сохранение файла в пользовательскую папку не подтверждалось; обычные выгрузки и повторный расчёт их содержимого проверены через API.

Это локальная проверка: публикация этой версии на GitHub/Render сюда не входит. Числа, даты и границы метода сохранены в RESPONSE.md, scenario_summary.md и results/.

## Фактическая проверка 2.2-evidence (2026-09-19)

Числа и трактовки 2.0/2.1 выше сохранены как история проверки. Для текущего решения применяются следующие результаты и методика [RESPONSE.md](RESPONSE.md).

- V01–V10 и E01–E32: 42/42. Дополнительно проверены запрет необеспеченной мощности, условное расширение, разделение подготовки/доставки C, сохранение платежей за бронь и TOP после сокращения заказа. Исходный репозиторий: 10/10 проверок целостности.
- Исходный BASE: NPV 8912,49. Неизменный STRESS и реакция только в пределах брони: NPV 9287,78, дефицит 147,83 т. Отдельный условный вариант новых контрактов: NPV 10122,91, дефицита и нарушений нет. Его доступность не гарантируется.
- Подготовленные варианты заранее обеспечивают B 110 т/год с 2038 и дополнительный физический буфер. Рекомендован «Усиленный резерв + подготовка»: BASE 8654,84; обязательный STRESS после реакции 10729,06, без дефицита и нарушений.
- Общий земной сбой A/B/C/E до 50% в 2038–2039 рассчитан отдельно: NPV 10400,96, дефицит 6,93 т, критический сервис 100%, нарушения резерва в 2039/2040. Доплата против подготовленной земной стратегии 202,58 млн NPV; сокращение общего дефицита 128,38 т. Критический сервис обеих подготовленных альтернатив 100%; преимущество по этому показателю не заявляется.
- Все 12 сохранённых реакций шести кандидатов повторно рассчитаны суточным ядром: NPV совпадает в пределах 1e-6. Четыре отдельных риска проверены с одной подготовленной стратегией и реакцией в пределах брони, остаточный дефицит нулевой. Это не проверка совместного наступления всех четырёх событий.
- API `/calculate` и обычные JSON/CSV/XLSX выгрузки проверены для пяти сохранённых планов: строгая и условная реакции, рекомендуемый BASE и его два сценария. Окна поставок присутствуют в выгрузке. Доля новой мощности 1,5 отклоняется HTTP 422. При подготовке C 18 месяцев и доставке 24 месяца первая поставка не раньше 2037.
- В браузере полный отчёт рассчитан заново: шесть кандидатов, рекомендация, все 42 контроля и числа совпали с артефактами. Проверены пять строк сравнения, реестр рисков, раскрытие чувствительности C и источников. Кнопка остаточных нарушений открыла исследовательский план с ID `TEAM_EARTH_COMMON_CAUSE`; показаны дефицит 6,93 т и два нарушения резерва.
- Вёрстка проверена при ширине 1440 и 390 px: горизонтального выхода страницы нет, таблицы прокручиваются внутри блока. Ошибок JavaScript в проверенных переходах нет. TypeScript и production-сборка пройдены; предупреждение о размере ECharts остаётся.

Полный аналитический отчёт занимает несколько минут; время и подтверждённость решения зависят от мощности сервера. Проверка локальная, публикация 2.2 на GitHub/Render не выполнена. Готовые финальные PDF/PPTX в эту проверку не входят: доступны текстовое обоснование и каркасы материалов.
